name: Python Tests

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master

jobs:
  run:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python_version: ["3.8", "3.9", "3.10", "3.11"]

    defaults:
      run:
        shell: bash

    steps:
    - uses: actions/checkout@v3
    - name: Download a file
      run: |
        wget --no-verbose https://datahub.io/machine-learning/mnist_784/r/mnist_784.csv -O mnist_784.csv
        mkdir data
        mv mnist_784.csv data/

    - name: Run tests
      run: |
        python -m pip install -r requirements_ci.txt
        git submodule init
        git submodule update
        python -m pip install --no-deps -e .
        coverage run -m pytest tests -vv

    - name: Coverage
      run: |
        coverage report -m
    - name: Pip freeze
      if: failure()
      run: |
        mkdir downloads
        python -m pip freeze > downloads/pip_freeze.txt
    - name: Upload frozen env
      if: failure()
      uses: actions/upload-artifact@v3
      with:
        name: pip-freeze
        path: downloads